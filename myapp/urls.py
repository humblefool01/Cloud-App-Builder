from django.urls import path
from . import views
from django.urls import include, path

urlpatterns = [
    #path('myapp/', views.index, name='index')    
    #path(r'^test/', views.request_page, name = 'test') 
]
